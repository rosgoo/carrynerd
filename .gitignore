# Raw upstream catalogues — regenerable via fetch.py, and large.
raw/
# Run logs
data/*.out
__pycache__/
*.pyc
.DS_Store

# SQLite artifacts — rebuilt from price-history.jsonl via `db.py sync`
data/*.db
data/*.db-wal
data/*.db-shm
